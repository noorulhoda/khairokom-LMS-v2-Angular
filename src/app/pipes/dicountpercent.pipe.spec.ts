import { DicountpercentPipe } from './dicountpercent.pipe';

describe('DicountpercentPipe', () => {
  it('create an instance', () => {
    const pipe = new DicountpercentPipe();
    expect(pipe).toBeTruthy();
  });
});
